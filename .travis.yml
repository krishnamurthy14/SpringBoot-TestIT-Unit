sudo: required
language: java
jdk: oraclejdk8
addons:
 sonarcloud:
   organization: krishnamurthy14
   token:
     secure:b5acbc50a3c20fa7c3038c9b9e8b55d1edacfb47

before_install:
- chmod +x mvnw

script:
- "./mvnw clean install -B"
- "./mvnw clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar"
cache:
 directories:
 - "$HOME/.m2/repository"
 - "$HOME/.sonar/cache"